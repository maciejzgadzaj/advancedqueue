langcode: en
status: true
dependencies:
  module:
    - advancedqueue_ui
  # Delete action when the module is uninstalled.
  enforced:
    module:
      - advancedqueue_ui
id: item_delete_action
label: 'Delete item'
type: advancedqueue_item
plugin: item_delete_action
configuration: {  }
