langcode: en
status: true
dependencies:
  module:
    - advancedqueue_ui
  # Delete action when the module is uninstalled.
  enforced:
    module:
      - advancedqueue_ui
id: item_requeue_action
label: 'Requeue item'
type: advancedqueue_item
plugin: item_requeue_action
configuration: {  }
