langcode: en
status: true
dependencies:
  module:
    - advancedqueue_ui
  # Delete action when the module is uninstalled.
  enforced:
    module:
      - advancedqueue_ui
id: item_reset_action
label: 'Reset attempt counter'
type: advancedqueue_item
plugin: item_reset_action
configuration: {  }
